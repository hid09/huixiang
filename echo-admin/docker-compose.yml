version: '3.8'

services:
  # 后端服务
  backend:
    build: ./backend
    container_name: echo-admin-backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    volumes:
      # 挂载回响 app 的数据库（只读）
      - /root/huixiang/echo-app/data/echo.db:/app/data/echo.db:ro
    networks:
      - echo-admin-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # 前端服务（含 nginx）
  frontend:
    build: ./frontend
    container_name: echo-admin-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - echo-admin-network
    depends_on:
      - backend

networks:
  echo-admin-network:
    driver: bridge
